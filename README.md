# Faucet fee token to L1 and L2

### Setup

```bash
yarn install
```

### Configuration

Copy `.env.example` into a `.env` file before running, see the variables, and change your private key.

### Faucet

```bash
yarn faucet
```
